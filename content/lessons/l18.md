---
title: SQL para QA
duration: 60 min
---

<h2>SQL para QA</h2>

<h3>🎯 Objetivos de Aprendizado</h3>
<ul>
  <li>Entender por que SQL é essencial para QA.</li>
  <li>Escrever consultas básicas com `SELECT`, `JOIN` e `GROUP BY`.</li>
  <li>Usar SQL para validar dados, comparar resultados e investigar bugs.</li>
  <li>Interpretar resultados e aplicar SQL em testes de qualidade.</li>
</ul>

<h3>🔍 Por que isso importa</h3>
<p>Mesmo em times de automação de UI, QA frequentemente precisa validar dados no banco, confirmar integrações e investigar discrepâncias entre frontend e backend.</p>

<ul>
  <li><strong>Validação de dados:</strong> checar se valores estão gravados corretamente.</li>
  <li><strong>Verificação de regras:</strong> confirmar limites, somas e agrupamentos.</li>
  <li><strong>Diagnóstico:</strong> investigar erros que aparecem apenas em produção.</li>
</ul>

<h3>📋 Nosso Banco de Dados de Exemplo</h3>
<p>Para entender e praticar as consultas abaixo, imagine que temos um banco de dados de e-commerce com as seguintes duas tabelas principais:</p>

<h4>Tabela: <code>usuarios</code></h4>
<table style="width:100%; border-collapse:collapse; margin:1rem 0">
  <tr style="background:#f5f5f5; border:1px solid #ddd">
    <th style="padding:0.5rem; border:1px solid #ddd">id (INT)</th>
    <th style="padding:0.5rem; border:1px solid #ddd">nome (VARCHAR)</th>
    <th style="padding:0.5rem; border:1px solid #ddd">email (VARCHAR)</th>
    <th style="padding:0.5rem; border:1px solid #ddd">status (VARCHAR)</th>
    <th style="padding:0.5rem; border:1px solid #ddd">criado_em (DATETIME)</th>
  </tr>
  <tr>
    <td style="padding:0.5rem; border:1px solid #ddd">1</td>
    <td style="padding:0.5rem; border:1px solid #ddd">Ana Silva</td>
    <td style="padding:0.5rem; border:1px solid #ddd">ana@email.com</td>
    <td style="padding:0.5rem; border:1px solid #ddd">ativo</td>
    <td style="padding:0.5rem; border:1px solid #ddd">2026-01-10 10:00:00</td>
  </tr>
  <tr style="background:#f9f9f9">
    <td style="padding:0.5rem; border:1px solid #ddd">2</td>
    <td style="padding:0.5rem; border:1px solid #ddd">Bruno Souza</td>
    <td style="padding:0.5rem; border:1px solid #ddd">bruno@email.com</td>
    <td style="padding:0.5rem; border:1px solid #ddd">inativo</td>
    <td style="padding:0.5rem; border:1px solid #ddd">2026-02-15 14:30:00</td>
  </tr>
  <tr>
    <td style="padding:0.5rem; border:1px solid #ddd">3</td>
    <td style="padding:0.5rem; border:1px solid #ddd">Carlos Oliveira</td>
    <td style="padding:0.5rem; border:1px solid #ddd">NULL</td>
    <td style="padding:0.5rem; border:1px solid #ddd">ativo</td>
    <td style="padding:0.5rem; border:1px solid #ddd">2026-03-20 09:15:00</td>
  </tr>
</table>

<h4>Tabela: <code>transacoes</code></h4>
<table style="width:100%; border-collapse:collapse; margin:1rem 0">
  <tr style="background:#f5f5f5; border:1px solid #ddd">
    <th style="padding:0.5rem; border:1px solid #ddd">id (INT)</th>
    <th style="padding:0.5rem; border:1px solid #ddd">usuario_id (INT)</th>
    <th style="padding:0.5rem; border:1px solid #ddd">valor (DECIMAL)</th>
    <th style="padding:0.5rem; border:1px solid #ddd">status (VARCHAR)</th>
    <th style="padding:0.5rem; border:1px solid #ddd">criado_em (DATETIME)</th>
  </tr>
  <tr>
    <td style="padding:0.5rem; border:1px solid #ddd">101</td>
    <td style="padding:0.5rem; border:1px solid #ddd">1</td>
    <td style="padding:0.5rem; border:1px solid #ddd">150.00</td>
    <td style="padding:0.5rem; border:1px solid #ddd">concluida</td>
    <td style="padding:0.5rem; border:1px solid #ddd">2026-07-30 11:20:00</td>
  </tr>
  <tr style="background:#f9f9f9">
    <td style="padding:0.5rem; border:1px solid #ddd">102</td>
    <td style="padding:0.5rem; border:1px solid #ddd">1</td>
    <td style="padding:0.5rem; border:1px solid #ddd">45.00</td>
    <td style="padding:0.5rem; border:1px solid #ddd">pendente</td>
    <td style="padding:0.5rem; border:1px solid #ddd">2026-07-31 08:00:00</td>
  </tr>
  <tr>
    <td style="padding:0.5rem; border:1px solid #ddd">103</td>
    <td style="padding:0.5rem; border:1px solid #ddd">3</td>
    <td style="padding:0.5rem; border:1px solid #ddd">300.00</td>
    <td style="padding:0.5rem; border:1px solid #ddd">concluida</td>
    <td style="padding:0.5rem; border:1px solid #ddd">2026-07-31 10:45:00</td>
  </tr>
</table>

<h3>📊 Consultas básicas</h3>
<pre style="background:#f5f5f5; padding:1rem; border-radius:0.5rem; overflow-x:auto">
-- selecionar colunas específicas
SELECT id, usuario_id, valor, status
FROM transacoes
WHERE status = 'pendente';

-- ordenar resultados
SELECT id, nome, criado_em
FROM usuarios
ORDER BY criado_em DESC
LIMIT 10;
</pre>

<h3>🔗 JOINs</h3>
<p>JOINs permitem combinar dados de tabelas relacionadas e são essenciais para QA quando você precisa validar um fluxo completo.</p>
<pre style="background:#f5f5f5; padding:1rem; border-radius:0.5rem; overflow-x:auto">
SELECT u.id AS usuario_id,
       u.email,
       t.id AS transacao_id,
       t.valor
FROM usuarios u
JOIN transacoes t ON t.usuario_id = u.id
WHERE t.status = 'concluida';
</pre>

<h3>📈 Agrupamento e agregação</h3>
<pre style="background:#f5f5f5; padding:1rem; border-radius:0.5rem; overflow-x:auto">
SELECT status,
       COUNT(*) AS total,
       SUM(valor) AS valor_total,
       AVG(valor) AS valor_medio
FROM transacoes
GROUP BY status;
</pre>

<h3>⚠️ Dados ausentes e regras de negócio</h3>
<p>SQL também ajuda a encontrar dados inconsistentes, valores nulos ou cenários que quebram a regra de negócio.</p>
<pre style="background:#f5f5f5; padding:1rem; border-radius:0.5rem; overflow-x:auto">
SELECT id, nome,
       COALESCE(email, 'SEM_EMAIL') AS email_validado,
       CASE
         WHEN status = 'ativo' THEN 'ok'
         ELSE 'revisar'
       END AS classificacao
FROM usuarios;
</pre>

<h3>💡 Casos úteis para QA</h3>
<ul>
  <li>Validar que um pedido cancelado não aparece como concluído.</li>
  <li>Comparar totais do frontend com o total do banco.</li>
  <li>Encontrar registros duplicados ou inconsistentes.</li>
  <li>Verificar que um campo crítico nunca fica nulo em produção.</li>
</ul>

<h3>❓ Perguntas de QA que SQL responde bem</h3>
<p>Em QA, SQL não é só para quem gosta de banco. Ele ajuda a transformar dúvidas de negócio em evidências verificáveis.</p>
<ul>
  <li>Quantos pedidos foram criados hoje e em que status ficaram?</li>
  <li>Existe algum valor inconsistente entre o total do pedido e a soma dos itens?</li>
  <li>Há usuários ativos sem transação associada?</li>
  <li>Os dados exibidos no frontend batem com o que está salvo no banco?</li>
</ul>
<pre style="background:#f5f5f5; padding:1rem; border-radius:0.5rem; overflow-x:auto">
SELECT pedido_id, SUM(valor_item) AS valor_total
FROM itens_pedido
GROUP BY pedido_id
HAVING SUM(valor_item) <= 0;
</pre>

<h3>⏭️ Próxima Aula</h3>
<p>Na próxima aula, veremos <strong>Testes de API</strong> e como validar endpoints, contratos e schemas.</p>

<h3>🐛 SQL para bugs comuns</h3>
<ul>
  <li>Verificar usuários com status inválido.</li>
  <li>Encontrar transações sem usuário associado.</li>
  <li>Detectar valores negativos em campos que não deveriam aceitar negativos.</li>
</ul>

<pre style="background:#f5f5f5; padding:1rem; border-radius:0.5rem; overflow-x:auto">
SELECT *
FROM transacoes
WHERE valor < 0;

SELECT *
FROM usuarios u
LEFT JOIN transacoes t ON t.usuario_id = u.id
WHERE t.id IS NULL
  AND u.status = 'ativo';
</pre>

<h3>🔒 Boas Práticas e Segurança de SQL para QA</h3>
<div style="background:#fef3c7; padding:1.5rem; border-radius:0.5rem; margin:1.5rem 0; border-left:4px solid #f59e0b">
  <ul style="margin:0; padding-left:1.2rem">
    <li>⚠️ <strong>Use bases de réplica:</strong> Sempre execute suas queries de validação em bancos de homologação ou réplicas de leitura (Read-Only) para evitar impactos de performance ou risco de alteração indevida.</li>
    <li>⚠️ <strong>Sempre use LIMIT:</strong> Em tabelas de produção ou com grande volume, limite o resultado (`LIMIT 100`) para não sobrecarregar ou travar o servidor de banco de dados.</li>
    <li>⚠️ <strong>Cuidado extremo com comandos de escrita:</strong> Nunca execute `UPDATE` ou `DELETE` em bancos de dados sem envolver em blocos de transação (`BEGIN TRANSACTION` e `ROLLBACK`) e sempre use cláusulas `WHERE` extremamente específicas (de preferência pelo ID primário).</li>
  </ul>
</div>

<h3>📝 Exercício Prático</h3>
<p>Escreva três consultas para validar um fluxo de pedido com base nas tabelas fictícias <code>pedidos</code> e <code>itens_pedido</code>:</p>
<ol>
  <li>Listar pedidos criados no último dia.</li>
  <li>Mostrar o total de pedidos por status.</li>
  <li>Listar pedidos cujo valor total não corresponde à soma dos itens (usando `JOIN` e `GROUP BY`).</li>
</ol>

<h3>✅ Gabarito (exercício)</h3>
<ul>
  <li><strong>Pedidos do último dia:</strong> <code>SELECT * FROM pedidos WHERE criado_em >= NOW() - INTERVAL '1 day';</code> — valida que o relatório mostra apenas pedidos recentes.</li>
  <li><strong>Total por status:</strong> <code>SELECT status, COUNT(*) AS total FROM pedidos GROUP BY status;</code> — verifica a distribuição de estados do fluxo de pedido.</li>
  <li><strong>Pedidos com total divergente:</strong> <code>SELECT p.id, p.total, SUM(i.quantidade * i.preco_unitario) AS soma_itens FROM pedidos p JOIN itens_pedido i ON i.pedido_id = p.id GROUP BY p.id, p.total HAVING p.total <> SUM(i.quantidade * i.preco_unitario);</code> — detecta inconsistências de cálculo.</li>
</ul>

<h3>📚 Recursos</h3>
<ul>
  <li><a href="https://www.w3schools.com/sql/" target="_blank" rel="noopener noreferrer">W3Schools SQL</a></li>
  <li><a href="https://sqlbolt.com/" target="_blank" rel="noopener noreferrer">SQLBolt</a></li>
</ul>
